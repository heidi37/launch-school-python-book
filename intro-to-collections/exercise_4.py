# Why can we treat strings as sequences?

# strings are text sequences
# the order is set, guaranteed
# strings are ordered and can be indexed, access individual characters with indexing
# print(string[1])
# the characters aren't objects, like in normal sequences
