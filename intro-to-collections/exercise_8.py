# How would you print all the numbers in the following range?

print(list(range(3, 17, 4)))

# the range isn't created until you use it, until you need them, when you iterate over them
# ranges are lazy sequences
